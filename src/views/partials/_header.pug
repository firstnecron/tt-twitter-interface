header.circle--header(style=`background-image: url(${userData.profile_banner_url})`)
	.bounds
		.circle--fluid
			.circle--fluid--cell
				a.logo(href="/")
					include ../../../public/images/twitter.svg
					h1 @#{userData.screen_name}
			.circle--fluid--cell.align-right
				nav
					ul.circle--inline
						li
							a Sign Out
